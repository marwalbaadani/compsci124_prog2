#include <iostream>

using namespace std;

int main(int argc, char **argv)
{
    // declare variables
    int flag = atoi(argv[1]);
    int dimension = atoi(argv[2]);
    int inputfile = atoi(argv[3]);
}